# PostgreSQL
POSTGRES_USER=app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=immo
DATABASE_URL=postgresql://app:changeme@db:5432/immo

# Optional — forwarded by the Next.js server when calling the FastAPI backend.
# BACKEND_API_TOKEN=